/**
 * Mail.ru OAuth was removed from runtime support.
 *
 * This empty module intentionally remains so overlay-based deployments cannot
 * resurrect an older adapter that still references the removed provider type.
 */
export {};
