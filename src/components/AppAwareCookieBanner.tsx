"use client";

import CookieBanner from "@/components/CookieBanner";

/** Cookie / analytics consent — same banner on web and native app shell. */
export default function AppAwareCookieBanner() {
  return <CookieBanner />;
}
