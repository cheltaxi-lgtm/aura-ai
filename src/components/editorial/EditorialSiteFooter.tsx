"use client";

import SiteFooter from "@/components/SiteFooter";

/** @deprecated Use SiteFooter variant="marketing" — kept as thin re-export. */
export default function EditorialSiteFooter() {
  return <SiteFooter variant="marketing" />;
}
