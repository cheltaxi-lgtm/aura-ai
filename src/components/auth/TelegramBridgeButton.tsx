"use client";

/**
 * Removed from UI: Telegram must not authenticate users (149-FZ art.8 p.10).
 * Account linking uses bot-issued link codes → /auth/telegram-link?code=…
 */
export default function TelegramBridgeButton() {
  return null;
}
