REVIEW: security
VERDICT: PASS
FINDINGS:
- **Webhook rate limit (`src/app/api/payment/webhook/route.ts`) вЂ” OK.** `clientIp` imported from `@/lib/api-guards` (correct source; trusts X-Forwarded-For only when `TRUST_PROXY=true`/production, and `deploy-prod.sh` forces `TRUST_PROXY=true` server-side). Limit (30/min per IP) sits at the top of `POST`, before content-type parsing. 429 carries `Retry-After` вЂ” `retryAfterSec` is always populated on denial in all three `checkRateLimit` paths (memory, PG `?? windowSec`, fail-closed). YuKassa/YooMoney retry on non-200, so a transient 429 loses no payment; comment in code documents this. No auth bypass: the limiter only rejects, and `verifyYoomoneyNotification` (sha1) / `processYukassaWebhook` calls are untouched. PG bucket query is parameterized (`$1/$2/$3`) and fails closed in production.
- **Dependencies (`package.json` / `package-lock.json`) вЂ” OK, residual-risk assessment confirmed (and conservative).** Lockfile matches claims: next 15.5.24, undici 6.28.0, puppeteer-core 24.43.1, postcss 8.5.26 (override applied), single top-level sharp 0.35.3 with no nested sharp entries. extract-zip 2.0.1 remains only via `@puppeteer/browsers@2.13.2 в†ђ puppeteer-core`. Verified actual usage: `src/modules/pro/pdf/render-pdf.ts` uses only `puppeteer-core.launch` against a pre-installed Chromium path, and `hosting/ensure-pro-pdf-chromium.sh` installs Chromium via `apt-get` вЂ” no repo code path invokes `@puppeteer/browsers`/extract-zip at all (no `browsers install` / Chrome-for-Testing download anywhere in scripts/, hosting/, src/). The CVE is therefore unreachable even at provisioning time as the script currently stands; accepting the residual risk is reasonable. Caveat: without shell access I could not diff old vs new lockfile, so "no downgrades/unexpected new deps" is verified by spot-check consistency with package.json ranges, not a full tree diff.
- **`scripts/deploy-prod.sh` вЂ” OK.** No secret leakage in new output (secrets are written to files with `chmod 600`, never echoed; `_BOT_SECRET`/`_SITE_SECRET` unset after use; only the non-secret `PRO_MODULE_ENABLED` flag value is echoed). Both health gates use `curl -s -o /dev/null -w '%{http_code}' --max-time 5/15` вЂ” sane. No injection vectors: quoted heredoc (`<<'REMOTE'`), quoted variables, repo-controlled paths. Installing `hosting/aura-ai.service` alongside the other units matches the prod-deploy drift-prevention rule. Cosmetic only: the "within 60s" message vs. worst-case ~210s retry loop (30 Г— (в‰¤5s curl + 2s sleep)) вЂ” not a security issue.
- **`src/lib/daily-reminder-service.ts` вЂ” OK.** `PREFERRED_HOUR_MSK_SQL` interpolates only `DEFAULT_REMINDER_HOUR_MSK = 9` (module-level numeric constant). User-controlled prefs are read inside SQL via `->>'...'` with `::int` casts (a malicious value errors the cast rather than injecting), and `hourMsk` is bound as `$1`. No injection surface.
- **CSS/UI/a11y/docs changes вЂ” OK.** No secrets and no client-side trust decisions (`isFree` / `billingExempt` / `guestResume` / token-bearing localStorage) in the changed components; `.gitignore` still ignores all env variants (`.env`, `.env.*`, `.env*.local`, `telegram-bot/.env`); `scripts/verify-human-design.mjs` contains no credentials.
- **P0 receipt/billing paths вЂ” NOT TOUCHED.** The diff contains no guest-resume/receipt/billing-service/auth files; free entitlement remains server-authoritative; webhook verification logic is unchanged. Receipt invariants (opaque token, hash-only DB, dual-cookie claim, no localStorage fallback) are unaffected by this changeset.
