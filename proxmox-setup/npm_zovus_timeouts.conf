# Paste into NPM → zovus.ru → Edit → Advanced → Custom Nginx Configuration
# Then Save and reload NPM.

client_max_body_size 10m;
proxy_read_timeout 180s;
proxy_send_timeout 180s;
proxy_connect_timeout 60s;
