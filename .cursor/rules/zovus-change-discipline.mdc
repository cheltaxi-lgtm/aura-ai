---
description: Change discipline — relevant callers, safe removals, product invariants and scoped checks
alwaysApply: true
---

# Zovus change discipline

## Before changing behavior or contracts

Inspect relevant callers, including dynamic use where applicable. Describe caller impact in the response when it matters to the user or reviewer; a caller inventory is not required for every function edit.

## Do not delete “unused” code

The repo has dynamic route handlers, feature flags, and server-provided deck configs. Apparent unused code is often loaded indirectly — leave it unless you prove zero references including dynamic imports and string routes.

## Deck size / pick count

Always take deck size and pick count from **server-provided** parameters / registry. Never hardcode `78` / `36` / `24` as product truth in client session logic.

## Motion

Animations: only `transform` / `opacity`. Honor `prefers-reduced-motion` (Capacitor/WebView battery).

## Timezones

Use IANA timezone strings. Do not encode business rules as UTC offset integers or `getTimezoneOffset()` math.

## Risky features

Ship behind an ENV kill-switch, **default off**.

## Verify

Run existing checks appropriate to the changed behavior. Reuse fresh passing evidence and avoid repeating checks already covered by the selected harness level. Run required preflight/release checks before an authorized deploy and fix relevant failures; read-only or documentation-only work does not require product tests.
