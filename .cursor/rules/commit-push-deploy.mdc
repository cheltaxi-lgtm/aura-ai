---
description: Commit, push, merge and production deploy within requested or already authorized release scope
alwaysApply: true
---

# Authorized commit → push → deploy

Perform only the commit, push, merge and deploy steps included in the request or standing authorization, once their required checks pass. Completing local work alone does not add release steps. For an authorized release:

1. **Commit** the task's logical changes when committing is authorized. Preserve unrelated work.
2. **Push** the branch and **fast-forward merge into `main`**, then push `main` (no force-push).
3. **Deploy production** with `scripts/deploy-prod.sh` via Git Bash (`C:\Program Files\Git\bin\bash.exe`), not WSL bash and not a hand-rolled wipe.

Do not ask again for authorization already provided in the session. If release authorization is missing and a release is requested, prepare the concrete reviewable change first; ask only for the missing action.

## Still respect

- Never force-push `main`/`master`.
- Never skip hooks unless the user explicitly asks.
- Never deploy destructive DB resets or secret rotations.
- If pre-push/invariants fail, fix and create a **new** commit (do not amend after reject).
- After deploy: check `aura-ai` + `aura-ai-async-jobs` active and `https://zovus.ru/api/health` → 200.
- If migration is part of the change, confirm `npm run migrate` ran on the server (deploy script does this).
