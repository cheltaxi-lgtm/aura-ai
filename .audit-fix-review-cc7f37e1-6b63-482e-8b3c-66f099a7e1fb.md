```
REVIEW: production
VERDICT: PASS
FINDINGS:
- prod-health: https://zovus.ru/api/health в†’ HTTP 200 ({"ok":true,"status":"ok"}). Production live, untouched by the uncommitted changes; smoke NOT REQUIRED.
- deploy-prod.sh health gates are correct under `set -euo pipefail`: `|| true` inside `$(curl ...)` prevents premature exit, `[ ... ] && break` is set-e-exempt, loop body ends with `sleep 2` so the for-loop exits 0 and the explicit `if ... exit 1` gates; REMOTE heredoc is 'REMOTE'-quoted (server-side expansion only); ssh propagates remote exit status so a failed remote gate aborts before the public gate вЂ” fail-closed.
- Gate targets verified: hosting/aura-ai.service binds 127.0.0.1:3000 (Type=simple, so the 30Г—2s retry loop correctly covers Next.js boot after `systemctl restart` and catches boot-loops that `is-active` would miss); Caddyfile reverse-proxies 127.0.0.1:3000 with handle_errors 502/503/504 в†’ /var/www/zovus-maintenance, consistent with the staged maintenance install before wipe.
- Unit install ordering correct: install aura-ai.service + aura-ai-async-jobs.service в†’ daemon-reload в†’ reset-failed (now including aura-ai) в†’ enable в†’ restart both app and worker; worker failure exits 1, satisfying the app+worker deploy contract.
- MINOR (pre-deploy fix recommended): the CRLF `sed -i 's/\r$//'` list (line 125) omits hosting/aura-ai.service while covering the other two units; .gitattributes only forces LF for *.sh, so a CRLF working-tree checkout could install a CRLF unit. Blast radius is a loud fail-closed deploy (health gate + maintenance page), not silent breakage. One-line fix: add hosting/aura-ai.service to the sed list or add `*.service text eol=lf` to .gitattributes.
- OBSERVATION: public gate is a single curl attempt from the operator machine (no retry) вЂ” a transient local network blip fails the deploy after a successful remote gate; conservative and acceptable.
- OBSERVATION (pre-existing, unchanged): `systemctl enable aura-ai` is never called (only async-jobs/telegram-bot); the existing enable symlink survives unit-file reinstall, so no drift in practice.
- README.md accurate: deploy-prod.sh via Git Bash documented as canonical; workflows dir contains preflight.yml + deploy.yml + android-debug.yml, ci.yml gone and unreferenced; described deploy flow matches the script.
- Lockfile consistent for server `npm ci`: next 15.5.24, undici 6.28.0, puppeteer-core 24.43.1 (all within package.json ranges), postcss override ^8.5.26 present, sharp only at 0.35.3 (no nested 0.34.5; linux-x64 platform packages present); local harness build already PASSED with this lockfile.
```
